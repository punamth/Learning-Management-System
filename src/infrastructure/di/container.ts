/*import ApiClient from "../api/apiClient";
import AuthRepositoryImpl from "../repositories/authRepositoryImpl";
import MovieRepositoryImpl from "../repositories/movieRepositoryImpl";
import { AuthService } from "../../application/services/authService";
import { MovieService } from "../../application/services/movieService";
import PaymentRepositoryImpl from "../repositories/paymentRepositoryImpl";
import { PaymentService } from "../../application/services/paymentService";

export const API_BASE_URL = "https://cinemav2.kutumbatech.com.np/ott_api"; 
const apiClient = new ApiClient(API_BASE_URL);
const authRepository = new AuthRepositoryImpl(apiClient);
const movieRepository = new MovieRepositoryImpl(apiClient);
const paymentRepository = new PaymentRepositoryImpl(apiClient);

export const authService = new AuthService(authRepository);
export const movieService = new MovieService(movieRepository);
export const paymentService = new PaymentService(paymentRepository);*/
